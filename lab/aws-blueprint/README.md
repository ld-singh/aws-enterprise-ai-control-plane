# AWS Blueprint

This folder will eventually contain reference IaC.

Do not deploy expensive AWS resources by default.

Planned options:

- Terraform module skeleton
- AWS CDK sample skeleton
- architecture-only AWS CloudFormation snippets
- AWS IAM policy examples
- Amazon VPC endpoint patterns
- observability wiring examples

## Rule

Every deployable component must have a cost warning and a teardown path.
